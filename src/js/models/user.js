 /*global define*/
define([
    'jquery',
    'backbone',
    'common',
], function($, Backbone, Common) {
    'use strict';
    var Model = Backbone.Model.extend({
        initialize: function() {
            //
        }
    });
    return Model;
});